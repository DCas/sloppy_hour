<h1>Nearby Venues</h1>
<ul>
<% @venues.each do |venue| %>
  <li><%= "#{venue.distance.round(2)} miles - " %><%= link_to venue.name, venue %></li>
<% end %>
</ul>

<p><%= @lat_lng %></p>

<%= link_to 'New Venue', new_venue_path %>