<%= simple_form_for @deal do |f| %>
  <%= f.association :venue, collection: @venues, label_method: :name, value_method: :id %>  
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>